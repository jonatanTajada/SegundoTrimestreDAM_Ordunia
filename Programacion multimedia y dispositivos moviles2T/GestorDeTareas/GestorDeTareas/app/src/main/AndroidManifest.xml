<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GestorDeTareas"
        tools:targetApi="31">


    <!-- MainActivity: Pantalla de inicio que abre el Dashboard -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <!-- DashboardActivity: Pantalla principal de la app -->
        <activity
            android:name=".DashboardActivity"
            android:exported="true"/>

        <!-- ListaTareasActivity: Vista para ver la lista de tareas -->
        <activity
            android:name=".ListaTareasActivity"
            android:exported="true"/>

        <!-- AgregarTareaActivity: Vista para agregar nuevas tareas -->
        <activity
            android:name=".AgregarTareaActivity"
            android:exported="true"/>

        <activity android:name=".AjustesActivity" android:exported="true"/>



    </application>
</manifest>
