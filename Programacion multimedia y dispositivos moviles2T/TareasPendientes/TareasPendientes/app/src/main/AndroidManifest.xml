<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TareasPendientes"
        tools:targetApi="31">

        <!-- 🚀 PANTALLA INICIAL: LOGIN -->
        <activity
            android:name=".view.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 🌍 REGISTRO DE USUARIOS -->
        <activity android:name=".view.RegistroActivity"/>

        <!-- 🏠 MENÚ PRINCIPAL -->
        <activity android:name=".MainActivity"/>

        <!-- 📋 GESTIÓN DE TAREAS -->
        <activity android:name=".view.TareasActivity"/>
        <activity android:name=".view.CrearTareaActivity"/>
        <activity android:name=".view.EditarTareaActivity"/>
        <activity android:name=".view.DetalleTareaActivity"/>

        <!-- ⚙️ CONFIGURACIÓN -->
        <activity android:name=".view.ConfiguracionActivity"/>

        <!-- ℹ️ INFORMACIÓN -->
        <activity android:name=".view.AcercaDeActivity"/>

    </application>
</manifest>
